# LLM 策略架构

LLM 策略通过大语言模型驱动交易决策，数据输入为完整历史数据，触发时机为每根 K 线，LLM 自主决定是否交易并返回可视化标注。

**为什么**：将 LLM 引入回测，让用户通过 Prompt 描述交易逻辑，实现策略与执行的解耦。同时支持与代码策略对比。

**替代方案考虑**：
- 滑动窗口输入 — LLM 足够智能，自主从完整历史数据中提取相关信息
- 固定交易点 — LLM 可能在多根 K 线后给出决策，不限制为单点

**后果**：LLM 调用成本较高，通过缓存机制优化；Annotation 数据需配套可视化工具渲染。