# 策略通过抽象基类（ABC）定义接口

用户实现策略，需继承 `Strategy` 基类并实现 `on_bar` 方法。生命周期钩子包括 `on_init` 和 `on_session_end`。

**为什么**：接口清晰、类型提示友好，便于 IDE 支持和扩展生命周期。

**替代方案考虑**：
- 回调函数/闭包 —— 灵活但分散，难以维护
- 生成器/数据流 —— 声明式但调试复杂

**后果**：策略编写者需要理解 Python 面向对象；当前不支持协程/异步模式。

## Status: Accepted