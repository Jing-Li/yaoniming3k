# 历史数据以 Parquet 格式存储，按日期/品种分区

行情数据（K线、分钟线）持久化为 Parquet 文件，目录结构为 `data/{symbol}/{freq}/{date}.parquet`。

**为什么**：列式存储压缩率高、查询快，适合 OLAP 场景。按日期分区符合时序数据访问模式。

**替代方案考虑**：
- SQLite —— 并发写入差，单文件限制
- 自定义二进制 —— 查询不灵活，难以迁移

**后果**：用户需要自行管理数据目录；首次运行时需要通过 Data Fetcher 准备数据。

## Status: Accepted