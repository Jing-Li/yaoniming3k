# 前端测试 stubs 引用不存在的 exports

- **ID**: 003
- **标签**: doc-sync, bug, closed
- **优先级**: LOW
- **状态**: closed

## 描述

前端测试中部分 stub/mock 引用了已被重构或删除的模块 exports，导致 9 个测试用例持续失败。这属于代码-测试不一致，也影响文档对测试状态的准确性描述。

## 验收标准

- [x] 修复测试 stubs 使其与当前模块导出一致
- [x] 前端测试全部通过

## 关联

- 相关 ADR: ADR-0010

## Agent Brief

> *This was generated by AI during triage.*

**Category**: bug | **State**: ready-for-agent

### Problem

2 个前端测试文件引用了源模块重构前的旧导出名称，导致 9 个测试全部失败。

### Reproduction

```bash
cd src/caisen/frontend && npx vitest run
# → 9 failed, Tests 9 failed (9)
```

### Root cause

| Test file | 旧 import | 实际 export (重构后) |
|-----------|----------|---------------------|
| `tests/js/utils.test.js` | `formatDate` | `formatTimestamp` |
| `tests/js/utils.test.js` | `formatNumber` | `formatValue(value, 'currency')` |
| `tests/js/utils.test.js` | `formatPercent` | `formatValue(value, 'percent')` |
| `tests/js/constants.test.js` | `COLORS` | `PATTERN_COLORS` |
| `tests/js/constants.test.js` | `CHART_CONFIG` | `LAYOUT` |
| `tests/js/constants.test.js` | `API_ENDPOINTS` | 不存在，需删除相关测试 |

### Files to modify

1. `src/caisen/frontend/tests/js/utils.test.js` — 更新 import 和测试用例以匹配当前 `formatValue`/`formatTimestamp` API
2. `src/caisen/frontend/tests/js/constants.test.js` — 更新 import 和断言以匹配 `PATTERN_COLORS`/`CHART_COLORS`/`LAYOUT`/`ANNOTATION_TYPES`，删除 `API_ENDPOINTS` 测试

### Reference files (read-only)

- `src/caisen/frontend/src/js/utils.js` — 当前导出: `formatValue`, `formatTimestamp`, `calculateAnnualReturn`, `isFiniteNum`, `isValidCoordPoint`, `isValidCoord`, `filterValidMarkPoints`, `filterValidMarkLines`, `PATTERN_COLORS`, `findBarByTimestamp`, `applyDateFilterToData`
- `src/caisen/frontend/src/js/constants.js` — 当前导出: `PATTERN_COLORS`, `CHART_COLORS`, `LAYOUT`, `ANNOTATION_TYPES`, `DEBUG_CONFIG`

### Acceptance

- `npx vitest run` 全部通过（0 failed）
- 不修改源模块，只修改测试文件
